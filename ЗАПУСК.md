# 🚀 КАК ЗАПУСТИТЬ ПРИЛОЖЕНИЕ

## Быстрый старт (3 клика!)

### Windows - Самый простой способ ✨

1. **Откройте папку приложения**
   - Перейдите в `c:\Users\User\Desktop\Alit\`

2. **Найдите файл `RUN.bat`** 
   - Это главный файл запуска

3. **Дважды кликните на `RUN.bat`** ✨
   - Всё запустится автоматически!
   - Браузер откроется сам
   - Нажимайте на кнопки в меню

---

## Альтернативные способы запуска

### Способ 2: PowerShell (более мощный)

```powershell
# Откройте PowerShell в папке приложения

# Запустить с меню
powershell -ExecutionPolicy Bypass -File launch.ps1

# Запустить сервер в фоне + открыть браузер
powershell -ExecutionPolicy Bypass -File launch.ps1 -Background

# Запустить тесты
powershell -ExecutionPolicy Bypass -File launch.ps1 -Test

# Очистить данные
powershell -ExecutionPolicy Bypass -File launch.ps1 -Clean
```

### Способ 3: Python напрямую

```bash
# Откройте CMD в папке приложения

# Интерактивное меню
python launch.py

# Автоматический запуск сервера
python launch.py --auto
```

### Способ 4: Классический способ

```bash
# Откройте CMD в папке приложения

# Установить зависимости (первый раз)
pip install -r requirements.txt

# Запустить сервер
python server.py

# В другом окне CMD:
python manage_server.py
```

---

## 📋 Что делает каждый файл запуска

| Файл | Что он делает | Как открыть |
|------|--------------|-----------|
| **RUN.bat** | ✨ Главный файл - запускает всё | Двойной клик |
| **launch.py** | 🐍 Python меню запуска | `python launch.py` |
| **launch.ps1** | 🔧 PowerShell менеджер | `powershell -File launch.ps1` |
| **start.bat** | 📋 Меню сервера | Двойной клик |
| **server.py** | 🚀 Сам сервер | `python server.py` |

---

## 🌐 После запуска

### В браузере откроется:
- Главная страница с чатом
- Форма входа/регистрации
- Чат с AI (ChatGPT)
- Глобальный мультиплеер чат

### Адрес:
```
http://localhost:5000
```

### Остановить сервер:
- Нажмите `Ctrl+C` в терминале где запущен сервер

---

## ⚙️ Менеджер сервера

В меню `RUN.bat` выберите пункт **2** для запуска менеджера, где можно:
- ✅ Проверить статус сервера
- 👥 Управлять пользователями
- 💬 Просмотреть сообщения
- 🔄 Перезагрузить сервер
- 🧹 Очистить данные

---

## 🧪 Тестирование API

Выберите пункт **3** в меню `RUN.bat` для запуска автоматического тестирования всех API эндпоинтов

---

## 📝 Первый запуск

1. ✅ Система автоматически установит зависимости
2. ✅ Проверит все файлы
3. ✅ Запустит сервер
4. ✅ Откроет браузер

**Готово! Приложение работает! 🎉**

---

## 🐛 Если что-то не работает

### "Python не найден"
- Установите Python с https://python.org
- При установке отметьте "Add Python to PATH"

### "Сервер не запускается"
- Проверьте, что порт 5000 свободен
- Откройте cmd и введите: `netstat -ano | findstr :5000`
- Если показано - убийте процесс или выберите другой порт в server.py

### "Браузер не открывается"
- Откройте вручную: http://localhost:5000
- Проверьте, что сервер работает (должно быть сообщение "Running on")

### "Не устанавливаются зависимости"
- Откройте cmd от администратора
- Выполните: `pip install -r requirements.txt`

---

## 🔑 API ключ OpenAI

Перед первым использованием чата:

1. Откройте файл `script.js`
2. Найдите строку: `this.apiKey = 'sk-proj-...'`
3. Замените на ваш ключ с https://platform.openai.com/api-keys

---

## 📂 Структура папок

```
Alit/
├── RUN.bat              ← 🚀 ЗАПУСТИТЕ ЭТОТ ФАЙЛ
├── launch.py            ← Python меню
├── launch.ps1           ← PowerShell меню
├── index.html           ← Главная страница
├── styles.css           ← Стили
├── auth.js              ← Система входа
├── script.js            ← Логика приложения
├── server.py            ← Flask сервер
├── manage_server.py     ← Менеджер сервера
├── test_api.py          ← Тестирование API
├── requirements.txt     ← Зависимости Python
├── config.json          ← Конфигурация
└── data/                ← Папка с данными
    ├── users.json       ← Пользователи
    └── messages.json    ← Сообщения
```

---

## 🎯 Кнопки в приложении

После входа в чате вы найдёте:

| Кнопка | Действие |
|--------|----------|
| **📝 AI Chat** | Чат с ChatGPT |
| **🌍 Global Chat** | Мультиплеер чат со всеми |
| **👤 Profile** | Профиль пользователя |
| **🚪 Logout** | Выход из аккаунта |

---

## 💡 Советы

- 🔒 Все данные хранятся локально в JSON файлах
- 🚀 Сервер слушает на `http://localhost:5000`
- 📊 При первом запуске может быть медленнее
- 🌙 Интерфейс поддерживает тёмный режим
- ♻️ История чатов сохраняется автоматически

---

## ✅ Всё готово!

**Просто запустите `RUN.bat` и наслаждайтесь! 🎉**

Если возникнут вопросы - проверьте логи в терминале сервера.

Happy chatting! 💬
